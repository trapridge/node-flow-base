{
  "name": "node-flow-base",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "babel --quiet --source-maps=true src/ -d dist/",
    "start": "babel-node src/",
    "start:release": "npm run build && node dist/",
    "flow": "flow",
    "lint": "eslint src/",
    "mocha": "mocha --require=babel-register -R spec --recursive --file test/config.js src/**/*.spec.js",
    "test": "npm run flow && npm run lint && nyc --include=src --exclude=**/*.spec.js --reporter=text --reporter=html --all npm run mocha",
    "watch:test": "npm run mocha -- --watch",
    "watch:lint": "esw -w src/",
    "watch:types": "flow-watch"
  },
  "author": "@trapridge",
  "license": "MIT",
  "engines": {
    "node": "0.9.4",
    "npm": "5.6.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^8.2.1",
    "babel-preset-env": "^1.6.1",
    "babel-preset-flow": "^6.23.0",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "eslint": "^4.16.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-flowtype": "^2.42.0",
    "eslint-plugin-prettier": "^2.5.0",
    "eslint-watch": "^3.1.3",
    "flow-bin": "^0.64.0",
    "flow-watch": "^1.1.1",
    "mocha": "^5.0.0",
    "nyc": "^11.4.1",
    "prettier": "1.10.2",
    "sinon": "^4.2.2",
    "sinon-chai": "^2.14.0"
  }
}
